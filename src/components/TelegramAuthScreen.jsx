import { Box, Typography } from '@mui/material';

const TelegramAuthScreen = () => {
  return (
    <Box sx={{ p: 3, color: '#fff', textAlign: 'center' }}>
      <Typography variant="h4">Telegram Auth Screen</Typography>
    </Box>
  );
};

export default TelegramAuthScreen;