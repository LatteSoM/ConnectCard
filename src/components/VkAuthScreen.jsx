import { Box, Typography } from '@mui/material';

const VkAuthScreen = () => {
  return (
    <Box sx={{ p: 3, color: '#fff', textAlign: 'center' }}>
      <Typography variant="h4">VK Auth Screen</Typography>
    </Box>
  );
};

export default VkAuthScreen;